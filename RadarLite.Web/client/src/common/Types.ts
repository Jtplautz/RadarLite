export interface Period {
  detailedForecast: string;
}

export interface ICity {
    id: string;
    name: string;
    //county: string;
  }

  export interface ILocation {
    id: string;
    name: string;
    //county: string;
  }
